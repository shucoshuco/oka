import { CellImagePipe } from './cell-image.pipe';

describe('CellImagePipe', () => {
  it('create an instance', () => {
    const pipe = new CellImagePipe();
    expect(pipe).toBeTruthy();
  });
});
